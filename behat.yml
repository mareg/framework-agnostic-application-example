default:
    gherkin:
        filters:
            tags: "~@wip&&~@disabled"
    suites:
        default:
            contexts:
                - FeatureContext: ~
    extensions:
        RMiller\BehatSpec\BehatExtension: ~
        Behat\Symfony2Extension: ~
